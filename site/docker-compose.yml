services:
  racingdna:
    build: .
    container_name: racingdna_app
    ports:
      - "80:5050"
    volumes:
      - fastf1_cache:/fastf1_cache
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  fastf1_cache:
